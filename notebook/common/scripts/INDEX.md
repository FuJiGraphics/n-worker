# scripts 인덱스 (common - 범용)

> 작업 중 만든 스크립트는 삭제하지 않는다. 재사용 가치가 있으면 이 폴더에 넣고 아래 형식으로 등록한다.
> 반복 작업을 시작하기 전, 여기(와 해당 프로젝트/스택 scripts)를 먼저 확인한다 - 이미 있으면 재사용한다.
> 등록 형식(전부 확정 지침 톤 - "~할 때 권장" 금지):
>
> - [이름](파일명) - 왜: <존재 이유> / 언제: <이 상황에서 쓴다> / 어떻게: <정확한 호출법> / 결과: <무엇이 나온다>

(아직 등록 없음)
