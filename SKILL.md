---
name: n-worker
description: >
  어느 프로젝트에서나 쓰는 공용 코드 작업 워커. 무거운 코드 작업(기능 추가, 동작 변경, 리팩토링,
  데이터 연결, 설계가 필요한 버그수정)을 인터뷰로 의도를 확정하고, 살아있는 플랜 파일을 사용자와
  함께 키우고, 생산 전에 적대 리뷰로 계획의 허점을 찾고, 생산-검증-지식 기록까지 완주하는 대화형
  워크플로우다. 3레이어 노트북(common/stacks/projects)으로 프로젝트별 컨벤션, 규칙, 맥락을 기억한다.
  사용자가 /n-worker 를 명시적으로 호출할 때 실행된다. 단순 질문, 한 줄 수정, 일회성 작업에는 쓰지 않는다.
disable-model-invocation: true
---

# n-worker - 공용 코드 작업 워커

어떤 프로젝트에서든, 요청받은 변경이 그 프로젝트의 구조와 컨벤션을 지키면서 의도대로 동작할 때까지 사용자와 끝까지 함께 진행하는 워크플로우다. 무거운 작업 전용이므로, 이 스킬에 진입했다는 것 자체가 이미 "의도 확정과 검증에 왕복을 쓸 가치가 있는 작업"이라는 뜻이다.

## 이 스킬 자신의 준칙 (본문과 산출물 모두에 적용된다)

- 이 파일은 35KB 를 넘기지 않는다(`wc -c` 로 확인). 넘치면 계층을 하나 더 만들어 점진 공개로 분리한다. 노트북 인덱스의 크기 임계는 [agents/curator.md](agents/curator.md) §5 가 단일 기준이다.
- **최소 지시 원칙:** 모델이 스스로 판단할 수 있는 부분은 지시하지 않는다. 지시는 판단이 실패하는 지점에만 둔다. 단 값과 임계(effort, 케이스 상한, 파일 수 경계)는 재량으로 열어두지 않는다 - 열어두면 결과를 검증할 수 없다.
- 지시는 명확하게 쓴다. 명확할 수 없는 지시는 명확해질 때까지 더 쪼갠다.
- 결정이 필요한 사용자 확인은 전부 `AskUserQuestion` 으로 한다. 근거로 추론한 선택지를 제시하고, "그 외"는 플랫폼이 자동으로 제공하므로 옵션에 직접 넣지 않는다. 줄글로 "어떻게 할까요?"라고 묻고 방치하는 것은 금지다. 단, 실행이나 관찰 결과를 자유 서술로 회신받는 핸드오프는 예외이며 [references/verify.md](references/verify.md) §3 의 체크리스트형 지시서를 쓴다.
- **모든 문항에 추천을 넣는다.** 첫 옵션이 추천안이고 라벨에 "(추천)"을 붙인다. 추천을 정하지 못하는 문항은 조사가 덜 된 것이므로, 묻기 전에 근거를 먼저 만든다. 근거를 다 모아도 우열이 없으면 무엇이 갈리는지를 직전 출력에 한 줄로 적고 **되돌리기 쉬운 쪽**을 추천으로 낸다. 선택지만 늘어놓고 판단을 사용자에게 넘기지 않는다.
- **긴 출력 뒤에는 같은 턴에서 바로 `AskUserQuestion` 을 호출한다.** 분석이나 플랜을 출력했으면 그 턴 안에서 질문까지 간다 - 출력만 하고 턴을 끝내면 사용자에게는 아무것도 안 물어본 정지로 보인다. 질문을 다음 턴으로 미루지 않는다. 오타 방지는 턴 분리가 아니라 **질문 본문을 짧게 유지**하는 것으로 한다 - 근거, 수치, file:line 은 직전 출력에 두고 질문 본문에는 결정문만 넣는다. 질문할 것이 없으면 그 자리는 확인 지점이 아니므로 멈추지 말고 진행한다.
- **노트북 대조 - 손댈 대상이 정해지면 그 이름으로 노트북을 먼저 grep 한다.** 대상은 "처음 보는 이름"이 아니라 **지금 읽거나 고치거나 근거로 삼을 파일, 시스템, 심볼, 에셋, 설정 키, 에러 문자열 전부**다. 이번 세션에서 이미 본 이름이어도 다시 건다 - P0 로딩은 인터뷰 전의 목록만 봤으므로 그때의 대조는 지금의 대상 집합과 다르다. **대조로 인정되는 것은 `<스킬 폴더>/scripts/nb-grep.sh "$WORK" <이름>...` 실행뿐이다** - 손 grep, Read, 기억은 대조가 아니다. 스크립트가 3레이어 인덱스를 전부 걸고, 히트한 lesson 본문을 출력에 함께 실으며, 대조 로그(`.active/<해시>.checks.log`)에 자동 기록한다. 조사 설계와 원인 추리보다 먼저다. **대조할 이름이 여럿 모였으면 한 번의 실행에 전부 넣어 건다**(다중 이름 지원).
- **대조 결과는 반드시 적는다. 찾은 것이 없으면 `히트 0` 이라고 적는다.** 빈칸은 "해당하는 게 없다"가 아니라 "확인 안 함"이다. 기록 위치는 플랜의 변경 설계 표([references/plan.md](references/plan.md) §1)이고 형식은 nb-grep 이 알려주는 `히트수 (log #행)` 이다. 플랜 파일이 없는 작업이면 답변에 한 줄로 적는다. 적히지 않은 대조는 하지 않은 것으로 취급한다. **도구 호출 없이 "대조 완료"라고 쓰지 않는다** - 앞서 읽어둔 기억은 대조가 아니다.
- **이 규율은 파이프라인이 끝나도 세션 내내 유효하다.** 한 세션에서 이 스킬을 한 번이라도 실행했으면, P5 를 끝낸 뒤 들어오는 추가 요청과 후속 수정에서도 파일을 고치기 전에 대조한다.
- PreToolUse 게이트(`scripts/nb-gate.sh`)가 등록된 기기에서는 대조 기록 없는 프로젝트 파일 수정이 거부될 수 있다(shadow 모드는 기록만). 게이트는 nb-load 가 마커에 적은 세션 id 로 n-worker 세션만 검사한다. 거부되면 nb-grep 후 재시도하고, 대조 대상이 아닌 파일(생성물, 메타 파일)이면 오탐으로 보고한다. **게이트가 없는 환경에서도 이 준칙은 유효하다.**
- 스택이나 프로젝트에 종속된 지식(예: "유니티에서는 이렇게")은 이 파일에 적지 않는다. 노트북의 해당 레이어에 적는다.
- **노트북은 전보체로 쓴다**(언어는 원래대로, 문체만 압축 - agents/curator.md 문체 절). 세션은 그대로 읽고, 풀어 쓴 옛 파일은 curator 가 만질 때 문체만 압축한다.
- **curator 는 이 스킬의 무의식이다.** 세션은 큐에 넣고 잊는다 - 기다리지 않고, 모니터를 걸지 않고, 결과를 읽거나 보고하지 않는다. 드러나는 것은 고장 한 줄뿐(`nb-load`). 사람이 원할 때만 `scripts/curator-ctl.sh`.
- **`notebook/` 아래의 쓰기는 curator 서브만 한다.** 생성, 수정, 이동, 아카이브 전부이며 등록·기록·스크립트 등록·정비가 모두 여기 해당한다. 메인은 노트북을 읽고 제안 목록만 만든다. 레이어 규칙·임계·점진 공개 판단이 한 파일([agents/curator.md](agents/curator.md))에만 있어야 규율이 갈라지지 않고, 노트북 전수 스캔이 메인 컨텍스트에 쌓이지 않는다.
- 사용자에게 보내는 답변과 지시문은 완결된 자연 문장으로 쓴다. 밀도 있는 압축 표기는 노트북 인덱스나 플랜 파일 같은 메모류에만 허용한다.
- **산출물은 `<스킬 폴더 절대경로>/scripts/open-artifact.sh "<파일 절대경로>"` 로 연다.** `open` 을 직접 호출하지 않는다 - 확장자 기본 앱으로 가서 md 가 편집 불가능한 앱에서 열린다. 스크립트가 반환하는 에디터 이름을 사용자 안내에 그대로 쓴다.
- 특정 모델의 버전 id 를 이 스킬의 어느 파일에도 박지 않는다. 모델은 세대 자동 추적 별칭(`'opus'`, `'sonnet'`, `'haiku'`)과 상대 배치 원칙으로만 지정하고, 어느 별칭이 어느 자리에 붙는지는 노트북([notebook/common/model-routing.md](notebook/common/model-routing.md))이 정한다 - 그 파일만 갱신 대상이므로 거기에는 현재 세대 확인용 id 가 적힌다.

## 작업 원칙

- **의도 파악이 가장 중요하다.** 모호한 부분을 임의로 판단해서 만든 결과물은 전부 재작업이 된다. 확신이 설 때까지 묻되, 이미 명확한 것은 묻지 않는다.
- **한 번에 끝나는 작업이 아니다.** 플랜 → 리뷰 → 생산 → 검증 → 수정을 의도가 수렴할 때까지 반복한다.
- **복제 후 적응이 생성보다 우선이다.** 처음부터 새로 짜지 않고, 그 프로젝트에서 가장 가까운 기존 구현을 본떠 차이 나는 부분만 바꾼다. 단 레퍼런스 자체가 컨벤션을 위반하고 있을 수 있으므로, 구조만 가져오고 스타일은 컨벤션 문서 기준으로 다시 검증한다.
- **읽은 것만 사실로 친다.** 시그니처, 컨벤션, 구조는 라이브로 읽은 것만 근거로 삼는다. 노트북도 "과거에 검증된 기록"일 뿐 현재 코드의 대체물이 아니므로, 어떤 값이나 구조에 동작을 걸기 전에는 라이브로 다시 확인한다.

## 노트북 - 3레이어 기억 (실행하면 무조건 먼저 읽는다)

이 스킬은 `notebook/` 폴더에 누적 지식을 가진다. 레이어는 "이 사실이 어디까지 일반화되는가"로 가른다:

| 레이어 | 내용 | 예 |
|---|---|---|
| `projects/<슬러그>/` | 프로젝트 고유 (구조, 진입점, 컨벤션 위치, 위임 경계, 서비스 연동) | "이 프로젝트의 피해 처리는 단일 진입점만 경유한다" |
| `stacks/<스택>/` | 엔진/아키텍처 일반 (unity, unreal 등). 항목마다 적용 버전을 명시한다 | "이 엔진의 에디터 API 는 들고 있던 참조를 무효화한다" |
| `common/` | 스택 무관 (도구 함정, 일반 원칙) | "Workflow args 페이로드는 조용히 유실된다" |

**로딩 절차 (매 발동):**
1. `<스킬 폴더 절대경로>/scripts/nb-load.sh <프로젝트 루트 절대경로>` 를 실행한다. 한 번의 호출로 registry 와 3레이어 INDEX(라우터)가 출력되고, `$WORK` 생성과 게이트 마커 기록까지 된다. 출력 머리의 `=== WORK:` 경로가 이 세션의 `$WORK` 다.
2. 미등록 프로젝트라면(출력에 `미등록` 표시) `AskUserQuestion` 으로 등록 정보를 받는다. 물을 것은 프로젝트 슬러그, 스택(없으면 `-`), 스택 버전, 이 프로젝트의 컨벤션/지침 문서 위치다. **쓰기는 메인이 하지 않는다** - 받은 답과 프로젝트 루트 절대경로를 요청 JSON(`mode: register`)에 담아 `scripts/curator-enqueue.sh <요청.json> --wait 600` 으로 curator 큐에 넣고 결과를 기다린다(형식은 [agents/curator.md](agents/curator.md) 큐 실행 형태 절). registry 행 추가와 3레이어 골격 생성은 curator 가 하고 메인은 done 요약만 받는다. 골격이 없으면 이후 기록이 붙을 곳이 없어지므로 이 투입은 생략하지 않는다. 요약을 받은 뒤 `nb-load.sh <루트> $WORK` 로 재실행해 새 레이어 인덱스를 로드하고 3번으로 간다.
3. nb-load 출력은 레이어별 INDEX.md(라우터)까지다. INDEX 의 안내를 보고 **이번 작업에 관련될 것 같은 도메인의 서브 인덱스(lessons-index-* 등)만** Read 로 열고, 나머지는 읽지 않는다. lesson 본문은 연 인덱스에서 해당하는 항목과 nb-grep 이 함께 출력하는 것만 읽는다. 전량 로드는 금지다 - 실행당 수만 토큰이 세션 내내 재전송되는 고정비가 된다. 버전 태그가 registry 의 프로젝트 버전과 맞지 않는 항목은 적용하지 않는다.
4. 인덱스 읽기는 서브에이전트에 위임하지 않는다. "어느 항목이 이번 작업과 관련되는가"는 판단의 문제라 메인의 몫이다.
5. **신선도 확인 둘.** 모델: [notebook/common/model-routing.md](notebook/common/model-routing.md) 머리를 보고 세션 모델 이름이 세대 매핑에 없거나 `checked` 가 60일 넘었으면 curator 를 `model-refresh` 로 큐에 넣는다(트리거 사유, 세션 모델 이름, 오늘 날짜 동봉). 하네스: nb-load 출력에 `하네스 버전 변경` 줄이 있으면 [notebook/common/harness-routing.md](notebook/common/harness-routing.md) 재검증을 curator `harness-refresh` 로 큐에 넣는다. `사용자 판단 대기 N건` 줄이 있으면 사용자에게 한 줄로 알린다. 어느 경우든 기다리지 않고 진행한다. 신선하면 조용히 지나간다.

**기록 게이트:** 기록할 가치가 있는 지식은 두 종류뿐이다. ① 이번에 조사 비용을 치르고 알아낸 것(다음에 또 치르지 않도록), ② 사용자가 말해줘야만 알 수 있는 암묵적/외부 지식(프로젝트의 목적, 정책, 외부 서비스의 사실). 코드를 열면 몇 초 만에 다시 확인되는 것은 기록하지 않는다. 기록과 정비는 P5 에서 curator 가 맡고, 메인은 제안 목록까지만 만든다.

## 단계 카드 - 스택/프로젝트 종속 절차를 주입하는 방식

파이프라인은 아래 P0~P5 가 전부다. 와이어링, 컴파일 검증, 플레이 검증처럼 스택에 종속된 절차는 고정 단계가 아니라 **카드**로 다룬다. 카드는 `stacks/<스택>/phases/` 와 `projects/<슬러그>/phases/` 에 "트리거 + 절차 + 수단" 형태로 기록되어 있다. P0 에서 INDEX 의 카드 목록을 보고 이번 작업에 관련될 것 같은 카드가 있으면 `AskUserQuestion` 으로 채택을 제안하고, 채택된 카드를 플랜의 해당 위치(카드가 스스로 명시한다)에 끼운다. 채택되지 않은 카드는 존재하지 않는 단계로 취급한다.

## 컨텍스트 경제 - 비용은 서브가 아니라 메인 누적에서 샌다

비용 규율의 핵심은 **메인 컨텍스트를 작게 유지하는 것**이다. 서브는 새 컨텍스트에서 시작하므로 큰일을 서브로 내리는 것 자체가 압축이다.

- **플랜에 없는 내용은 서브에게 전달되지 않는다.** 서브는 메인의 대화를 이어받지 않고 작업 명세(플랜 발췌, 파일 경로, 규칙 목록)만 받는다. 위임하려는 일의 판단 재료가 플랜에 없으면 위임 전에 플랜에 옮겨 적는다. 작업 명세를 적다가 대화에서 한 얘기를 풀어 설명하고 싶어지면, 그 일은 아직 위임할 수 없는 상태이거나 그 얘기가 플랜에 누락된 것이다.
- **파일 본문은 작업 명세에 붙이지 않는다.** 경로와 file:line 만 싣고 서브가 라이브로 읽게 한다. 스폰 프롬프트도 같다 - 명세는 `$WORK` 에 파일로 쓰고 경로만(렌즈는 플랜 경로 + 3줄 요약, curator 는 요청 JSON, reporter 는 명세 파일). 반환 규격에 "따른 명세 항목" 을 넣어 읽었는지 확인한다. 예외는 아직 파일이 아닌 것(코드 초안, 수용 리스크 목록)뿐이며 이것들은 리터럴로 싣는다.
- **위임할 수 없는 일.** 사용자 의도의 미묘한 부분이 관련된 판단, 우려의 취합, 대화는 메인 몫이다. 입력을 파일과 작업 명세로 완결되게 적을 수 있는 일(조사, 렌즈 리뷰, 독립 모듈 생산, 로그 판독)은 전부 위임 후보다.
- **메인 컨텍스트 관리.** 대량 출력을 메인에 들이지 않는다: Workflow 는 건수만 `return` 하고 전문은 `scripts/wf-summarize.py <journal.jsonl>` 로 읽는다(근거는 harness-routing.md #4, #6). 빌드와 테스트 출력은 파일로 리다이렉트해 발췌만 읽는다([references/verify.md](references/verify.md) §2). 500줄 넘는 파일은 필요한 절만 부분 Read 한다. 서브 반환은 요점 규격으로 제한한다(diff 전문과 로그 원문 반환 금지). 플랜 갱신은 바뀐 절만 고친다([references/plan.md](references/plan.md) §1).

## 서브에이전트 운용 (모델, effort, 구조)

대화, 판단, 취합, `AskUserQuestion` 은 전부 메인(이 세션)의 몫이고 위임할 수 없다. 서브는 무거운 조사, 독립 리뷰, 대형 생산, 로그 판독에 쓴다.

**오케스트레이션 기본 원칙:**

- **메인은 사용자에게 열려 있어야 한다.** 실질 작업은 위임으로 내리고, 메인은 대화, 판단, 취합, 승인 왕복을 지킨다. 메인이 긴 단독 작업에 잠기면 사용자 개입 지점이 사라진다.
- **사전 조사는 좁게 갈라 병렬로 돌린다.** 읽기 전용 조사(존재, 위치, 시그니처 확인)는 항목을 좁게 나눠 병렬 스폰한다. effort 기본값은 [references/review.md](references/review.md) §4 가 정한다.
- **담당 범위 분리.** 서브마다 범위를 겹치지 않게 가른다 - 같은 파일, 같은 질문을 두 서브에 주지 않는다. 예외는 의도적 교차 검증뿐이다.
- **서브에이전트는 다시 위임하지 않는다.** 모든 서브에 "범위를 넘으면 범위 초과와 분할 제안만 보고하라"를 싣고, 재분할은 메인이 한다. 위임 깊이를 1단으로 고정해야 비용과 실패 지점이 늘지 않는다.
- **서브는 상태를 유지하지 않는다.** 매번 새 컨텍스트에서 작업 명세만 받아 일하고 요점만 반환하고 종료한다. 이전 맥락의 영향 없이 보게 하는 것이 목적이다.
- **모델과 effort 값은 표에서 읽어 쓴다.** 모델 배치는 [notebook/common/model-routing.md](notebook/common/model-routing.md) §1~2, 렌즈 effort 는 [references/review.md](references/review.md) §0, P1 조사 effort 는 §4 다. model-routing 은 P0 신선도 확인 때 읽은 것을 세션 내내 쓴다 - 파일이 바뀌지 않는 한 스폰 때마다 재독하지 않는다(재독은 같은 내용을 컨텍스트에 또 쌓는다). 이 파일에는 모델 이름, 버전, 티어, effort 값을 적지 않는다.
- **표 값이 기본값이다.** 벗어난 값을 쓸 때만 플랜 결정 로그에 근거 한 줄을 남긴다(회귀 표면, 실패 비용, 그 서브가 실제로 할 일의 무게 - 대조 대상이 3개인지 30개인지). 근거 없는 이탈은 표 값으로 되돌린다.
- **`model` 은 항상 명시한다.** 생략하면 세션 모델을 상속해 전 서브가 최상위 티어로 돈다. 세션 모델과 동급 이상이 필요하면 먼저 `AskUserQuestion` 으로 승인받는다.
- **`effort` 는 Workflow `agent()` 에서 명시한다** - `Agent` 도구에는 effort 파라미터가 없어 세션 값을 상속한다(harness-routing.md #8). 예외는 사용자 대면 산출물을 만드는 생산 위임(P3 생산, P5 reporter)으로, 생략해 세션 effort 를 상속하고 세션이 `max` 면 `xhigh` 를 명시해 내린다. 상한과 근거는 model-routing.md §3.
- **교차 검증:** 렌즈 묶음 하나가 1세트다. 난이도가 높으면 같은 세트를 2~3개 복제해 독립 실행하고 결과를 대조할 수 있다. 1세트를 넘는 투입은 토큰 비용이 사용자 몫이므로 `AskUserQuestion` 으로 승인받는다.
- **동시 2개 이상 스폰은 `Workflow` 다**(이 스킬의 지시가 곧 opt-in). P1 병렬 조사, P2 렌즈, P3 병렬 위임 생산 전부. `Agent` 는 단일 위임 전용이고, Workflow 가 없는 환경에서만 병렬 폴백으로 쓴다. 읽기 전용 조사는 `agentType: 'Explore'`. 선택 근거와 폴백 보완은 [references/review.md](references/review.md) §4.
- **신뢰 규율:** 서브 보고는 file:line 으로 받는다. **코드나 플랜을 바꾸게 되는 항목은 전부** 라이브 재확인하고, 바꾸지 않는 항목은 재확인 없이 플랜 리스크 절에 기록만 한다. 문자열 키, 경로, id 는 컴파일러가 실재를 보증하지 못하므로 예외 없이 확인한다.
- **실패 처리:** 서브가 에러나 타임아웃으로 죽으면 그 단위만 1회 재시도하고, 그래도 안 되면 메인이 직접 한다. 시작 후 몇 초 만에 도구 사용 0회로 끝난 서브도 실패로 간주한다. **curator 는 이 폴백에서 제외된다** - done 파일이 `failed` 나 `denied` 면 노트북 쓰기를 포기하고, 무엇을 기록·등록하려 했는지 사용자에게 한 줄로 보고한다(반쯤 쓰인 노트북이 안 쓰인 노트북보다 나쁘다). 단 P0 등록 실패(`--wait` 초과 포함)는 진행을 막으므로 이때는 사용자에게 `AskUserQuestion` 으로 "재시도 / 이번 세션은 노트북 없이 진행"을 묻는다.

## 진행 순서

```
P0 컨텍스트   nb-load(registry+3레이어 인덱스+$WORK+마커) → 미등록이면 curator register → 무게/위임 경계 확인 → 추적/카드 확인
P1 인터뷰     질문 ↔ 답 ↔ 플랜 갱신 ↔ (필요 시) 병렬 조사 - 수렴까지 반복  [프로젝트 파일 편집 잠금]
P2 사전 리뷰   난이도 판정(렌즈 2/5/7+) → 적대 리뷰 + 정적 추적 → 취합 → 플랜 렌더 + 승인
P3 생산       복제 후 적응 - 대형 묶음은 작업 명세로 위임, 소형은 메인 직접 (+ 스크립트 보존)
P4 검증       자동 검증 + 채택 카드 - 로그는 발췌로만, 의도 수렴까지
P5 기록과 보고  curator 기록/스크립트 등록(신호 있을 때만) → 변경 표 → 보고서 질문 → reporter 스폰(pdf/html)
```

Phase 사이의 전환은 1~2줄 진행 보고만 하고 멈추지 않고 넘어간다. 사용자 확인은 설계된 지점에만 둔다: P0 의 질문, P1 인터뷰 라운드, P2 의 우려와 플랜 승인(렌즈 구성은 중 난이도에서만, 그것도 인터뷰 문항에 합쳐서), P4 핸드오프(채택 카드), P5 보고서 여부.

**멈춤은 질문과 함께여야 한다.** 확인 지점에서는 그 턴 안에서 `AskUserQuestion` 까지 호출하고, 확인 지점이 아니면 보고 한 줄만 남기고 계속 간다. 질문 없이 턴을 끝내는 것은 어느 경우에도 정답이 아니다.

## P0 - 컨텍스트

1. 위의 노트북 로딩 절차를 수행한다. 미등록 프로젝트의 등록 질문도 여기서 한다.
2. **무게와 위임 경계를 먼저 확인한다.** 작업이 가볍다면(한 줄 수정, 단순 질문) 이 파이프라인이 과하므로 스킬 없이 바로 처리할지, 위임 경계(프로젝트 facts 에 전용 스킬이 명시된 영역)에 걸치면 양보할지를 `AskUserQuestion` 으로 확인한다. 둘 다 해당 없으면 이 단계는 조용히 지나간다.
3. 세션 작업 폴더는 nb-load 가 이미 만들었다(로딩 절차 1의 `=== WORK:` 줄). **그 절대 경로를 기억하고, 이후의 모든 명령과 작업 명세와 문서에 그 리터럴 경로를 치환해 쓴다**(셸 변수는 호출 사이에 유지되지 않는다). 이 스킬의 모든 문서에서 `$WORK` 표기는 그 절대 경로를 뜻한다. 경로는 플랜 헤더에도 적는다.
4. `AskUserQuestion` 한 번으로 두 가지를 묻는다:
   - **플랜 추적** - "진행 중 플랜 파일을 계속 확인하시겠습니까?" ① md 파일(열어준다) ② html(다이어그램/코드 시각화 병행) ③ 아니오(대화로만). 어느 답이든 `$WORK/plan.md` 자체는 **항상 유지한다** - 플랜이 스펙의 원본이고 긴 세션의 컨텍스트 압축을 견디는 장치이기 때문이다. 이 질문은 보여주는 방식만 정한다. ①이나 ②를 고르면 경로를 알리고 `scripts/open-artifact.sh` 로 연다(준칙의 산출물 열기 규칙 - 어느 에디터로 열렸는지도 함께 알린다). ②를 골랐을 때만 [references/report-render.md](references/report-render.md) §1~2 를 읽어 plan.html 을 만든다 - md 추적이면 그 파일은 로드하지 않는다.
   - **단계 카드 채택** - 3레이어 INDEX 의 카드 중 이번 작업에 걸릴 것들만 골라 "이 단계들을 플랜에 넣을까요?"(multiSelect). 해당하는 카드가 없으면 이 문항은 생략한다.
5. 작업 브리핑이 이미 명확하면 바로 P1 로 간다.

## P1 - 인터뷰 (플랜 반복 갱신) - 가장 중요한 단계다

`$WORK/plan.md` 가 곧 작성 중인 스펙이다. 결정 트리 순회, 질문 기법, 병렬 조사, 수렴 판정은 [references/interview.md](references/interview.md) 가 단일 기준이다.

이 절에만 있는 규칙:

- **편집 잠금.** P1~P2 구간에서 프로젝트 파일은 읽기만 한다. 쓰는 파일은 플랜과 `$WORK` 산출물뿐이고, 프로젝트 파일 편집은 P2 승인 뒤에 시작한다.
- **전제 충돌 처리.** 조사 결과가 요청의 전제와 모순되면(요청한 변경이 이미 구현되어 있다, 바꾸려는 대상이 없다) 계속 진행하지 않는다. 검증 근거(file:line)와 함께 사용자에게 올리고 방향을 확인한다. "이미 되어 있으므로 작업 불필요"도 정당한 종료다 - P5 로 건너뛰어 근거를 보고한다.

## P2 - 사전 적대 리뷰 (생산 전 1사이클)

코드를 쓰기 전에 깬다. 생산 후의 정적 리뷰는 없다. 그래서 이 단계의 입력은 "쓸 코드"여야 한다: **파일별 변경 계획과 핵심 코드 초안**을 플랜에 먼저 적는다. 전문이 아니라 구조, 시그니처, 핵심 분기가 보이는 수준이면 된다.

난이도별 신호, 렌즈 구성, 렌즈 정의와 임무문, 카탈로그, ④ 케이스 상한, effort 는 [references/review.md](references/review.md) §0~1b 가 단일 기준이다. 스폰 전에 그 표를 읽는다.

이 절에만 있는 규칙:

- **판정 시점은 코드 초안을 플랜에 적은 뒤다** - 신호인 파일 수는 초안이 있어야 확정된다. 파일 수는 생성하거나 수정하는 파일 전부를 센다(코드, 프리팹, 에셋, 설정 불문 - 위험은 코드 밖에서도 온다).
- **상향 신호와 경량 신호가 충돌하면 상향이 이긴다.** 클론 매핑은 코드 작성 난이도를 낮출 뿐 회귀와 함정 위험을 낮추지 않는다.
- "더 나은 방식이 있는가"는 렌즈가 아니라 **메인의 몫**이다. 취합하면서 스스로 묻고, 있으면 대안으로 올린다.
- **취합은 메인이 한다**(재확인 범위는 준칙의 신뢰 규율). ② 의 우려와 가치 충돌(더 단순한 대안 vs 기존 구조)은 `AskUserQuestion` 으로 사용자에게 올리되 추천안과 근거를 첫 옵션에 명시한다.
- 치명적 지적이 나오면 해당 지점만 P1 로 되돌아가 다시 수렴하고 다시 리뷰한다. 재실행 횟수는 플랜의 결정 로그에 남긴다(게이트 상한은 가드레일).
- **승인 문항을 내기 전에 노트북 대조를 끝낸다**(준칙의 노트북 대조 규율). 대상은 변경 설계 표의 파일과 심볼 전부이고, 행마다 대조 칸을 채운다. **대조와 별개로, 변경과 관련된 스택/프로젝트 도메인의 서브 인덱스(INDEX 라우터가 안내하는 lessons-index-* 류)를 최소 1회 열었는지 이 시점에 확인하고, 연 인덱스 목록을 플랜에 한 줄 적는다.** nb-grep 은 이름이 있어야 검색된다 - 이름을 모르는 함정은 인덱스 훑기로만 발견된다. 안 열었으면 지금 연다(해당 도메인 1~2개면 충분하다). 인터뷰와 리뷰에서 들어온 대상은 P0 로딩이 보지 못했으므로 여기서 처음 검색된다. **칸이 하나라도 비어 있으면 승인 문항을 내지 않는다.** 검색된 lesson 은 이 자리에서 본문을 열어 설계에 반영하고, 설계가 바뀌면 그 행의 칸을 비우고 다시 대조한다.
- **플랜 승인.** 통과하면(또는 사용자가 우려를 수용하면) 플랜을 대화에 직접 출력하고 최종본을 파일에 반영한 뒤, **같은 턴에서 이어서** `AskUserQuestion` 으로 승인받는다. 파일만 열어 두고 줄글로 넘어가는 것도, 플랜만 출력하고 턴을 끝내는 것도 금지다. 출력 분량은 요약 고정이다: 변경 설계 표, 코드 초안 핵심, 리스크만 대화에 출력하고 전문은 파일 경로로 안내한다 - 플랜 전문이 파일과 대화록에 이중으로 쌓이면 그 분량이 세션 내내 재전송된다. 예외는 플랜 추적을 ③(대화로만)으로 고른 세션뿐이다: 사용자가 전문을 볼 통로가 대화밖에 없으므로 전문을 출력한다. 첫 옵션은 "이 플랜으로 생산 시작"이고, 거부되면 해당 지점만 재수렴한다. 수용된 우려는 이후 서브 입력에 "알려진 수용 리스크"로 담아 같은 지적이 반복되지 않게 한다.

## P3 - 생산

위임 판단, 작업 명세 규격, 모듈 경계 계약, 불일치 보고, 복제 후 적응 절차, 컨벤션 점검, 스크립트 보존은 [references/produce.md](references/produce.md) 가 단일 기준이다.

이 절에만 있는 규칙:

- **위임 임계는 파일 3개다.** 3개 이상을 만들거나 고치는 묶음, 또는 다른 부분과 파일이 겹치지 않는 독립 모듈은 위임한다. 1~2파일이나 방금 합의된 미묘한 디테일이 관련된 수정은 메인이 직접 한다.
- **착수 직전 플랜의 대조 칸을 확인한다.** 빈 행이 있거나 승인 뒤에 표가 바뀌었으면 그 행만 지금 대조해 칸을 채운 뒤 시작한다. 검색된 본문은 메인이 열고, 위임 작업 명세에는 그 경로를 싣는다(히트 0 이면 `없음` 이라고 명시한다 - 항목 자체를 빼지 않는다).
- 생산 직후 위치로 채택된 단계 카드가 있으면 여기서 카드 절차대로 실행한다.

## P4 - 검증

자동 검증 수단 결정, 로그 판독, 핸드오프 형식, 반복 규칙은 [references/verify.md](references/verify.md) 가 단일 기준이다.

이 절에만 있는 규칙:

- 이 시점에 배치된 채택 카드를 카드 절차대로 실행한다. 각 카드가 자기 실행 위치를 명시하며, 생산 직후 카드는 P3 에서 이미 실행되었다.
- 요청한 변경이 의도대로 동작할 때까지 반복한다. 완료 판정 기준은 verify.md §4.

## P5 - 기록과 보고

1. **기록 판정 (메인):** 위 기록 게이트를 통과하는 지식이 생겼는가, 또는 등록할 스크립트가 남았는가? 둘 다 없으면 노트북 작업을 생략한다(대부분 이 경우다). **이 스킬 자체를 시험하는 세션(eval/dogfood)이면 기록을 전부 생략한다** - eval 에서 드러난 결함은 스킬 파일로 고칠 문제이지 도메인 사실이 아니다.
2. 둘 중 하나라도 있으면 요청 JSON 을 `$WORK` 에 쓰고 `scripts/curator-enqueue.sh <요청.json>` 으로 **curator 큐**에 넣는다(세션 서브로 스폰하지 않는다 - 데몬이 별도 프로세스로 큐를 비운다, [agents/curator.md](agents/curator.md) 큐 실행 형태 절). JSON 의 `payload` 에 실을 것: 모드(기본 `record`), 기록 제안 목록(항목별 내용 요약/제안 레이어/근거), 스크립트 등록 제안(경로와 재사용 근거) - 프로젝트 루트,슬러그,스택,호출 세션 이름은 최상위 키. 기록, 스크립트 등록, 정비(노후화 재검증, 승격/강등, 임계 분리)를 전부 curator 가 한다 - 기록거리가 없고 스크립트만 남았을 때도 메인이 직접 쓰지 않는다(노트북 쓰기 단일 주체). **기다리지 않고 결과를 보고하지도 않는다** - curator 는 뇌의 기억처럼 뒤에서 말없이 정리하는 기계다(사용자 결정 2026-09-03). 큐 투입 한 줄만 남기고 P5 를 계속한다. `nb-load` 는 실패(`status: failed`)가 있을 때만 한 줄 알린다. 사람이 원할 때만 `scripts/curator-ctl.sh status|results|stop`.
3. **변경 요약 표**를 보고한다: | 파일 | 내용 | 영향 |. 프로젝트 지침에 별도 보고 형식이 있으면 그것이 우선한다.
4. **보고서:** 3번의 변경 표를 렌더한 뒤 같은 턴에서 이어서 `AskUserQuestion` 으로 "결과 보고서를 남길까요?"를 묻는다 - ① pdf 로 저장(보관 관례 위치) (추천) ② html 만 ③ 아니오. 예라면 **reporter 서브**를 스폰한다([agents/reporter.md](agents/reporter.md) 를 읽혀서 - 배치는 model-routing.md §3). 작업 명세에 실을 것: `$WORK` 경로, 스킬 폴더 경로, 출력 대상, 저장 관례([notebook/common/report-conventions.md](notebook/common/report-conventions.md) 에서 - 항목이 비면 보고서 질문에 저장 위치를 함께 묻고 답을 curator 기록 후보로 넘긴다), 프로젝트 루트, 오늘 날짜, 핸드오프 회신 요약(플랜 검증 절에 없으면 리터럴로). 반환의 **재료 부족 목록**이 비어 있지 않으면 해당 절만 플랜을 보충해 1회 재스폰한다. 받은 경로를 `scripts/open-artifact.sh` 로 열고 안내한다.
5. **세션은 여기서 끝나지 않는다.** 보고까지 마친 뒤 들어오는 추가 요청과 후속 수정도 파일을 고치기 전에 노트북 대조를 한다(준칙). 플랜 파일이 없는 짧은 작업이면 대조 결과를 답변에 한 줄로 적고, 무거운 새 작업이면 P0 부터 다시 탄다.

## 가드레일 (전 구간)

- **기존 구조 이탈 금지.** 노트북과 라이브 확인으로 파악된 프로젝트의 진입점과 구조 밖에 새 우회로, 중복 시스템, 하드코딩 키를 만들지 않는다. 프로젝트 facts 가 "불변"으로 명시한 위반은 사용자도 넘길 수 없다 - 선택지는 "포기 또는 구조 안으로 수정"뿐이다.
- **스택/프로젝트 종속 지식을 이 파일과 common 레이어에 쓰지 않는다.**
- **게이트 상한:** 같은 게이트를 2회째 재실행하게 되면 `AskUserQuestion` 으로 묻는다. 무한 루프 금지.
- **자동 커밋 금지.** 커밋은 사용자가 시킬 때만 한다.
- **위임 경계:** 프로젝트 facts 에 명시된 전용 스킬의 영역은 그쪽에 양보한다.
- **지침-환경 미스매치 표면화:** 프로젝트 지침이 요구하는 도구나 스킬이 환경에 없으면 조용히 건너뛰지 말고, 시작할 때 "없어서 생략한다"고 한 줄로 알린다.

## 참조 파일

- [notebook/registry.md](notebook/registry.md) - 경로 → 프로젝트/스택 매핑. 실행 시 nb-load.sh 출력으로 읽힌다.
- [references/interview.md](references/interview.md) - 인터뷰 절차, 질문 설계, 병렬 조사, 수렴 판정.
- [references/plan.md](references/plan.md) - 플랜 파일 포맷.
- [references/report-render.md](references/report-render.md) - html 렌더와 보고서 규격. html 추적 모드의 메인과 reporter 만 읽는다.
- [references/review.md](references/review.md) - 난이도 판정, 렌즈 정의와 카탈로그, 교차 검증, Workflow 골격.
- [references/produce.md](references/produce.md) - 복제 후 적응, 위임 생산(작업 명세/경계 계약), 컨벤션 점검, 스크립트 보존.
- [references/verify.md](references/verify.md) - 검증 절차, 로그 판독, 핸드오프 형식.
- [notebook/common/harness-routing.md](notebook/common/harness-routing.md) - 이 스킬이 기댄 하네스 동작 목록과 재검증 트리거. nb-load 가 버전 변경을 알린다.
- [agents/curator.md](agents/curator.md) - 노트북 쓰기 단일 주체의 지침 (등록, 기록, 정비, 언어, 임계).
- [agents/reporter.md](agents/reporter.md) - 결과 보고서(html/pdf)를 생성하는 서브의 지침. 메인은 스폰과 경로 안내만 한다.
