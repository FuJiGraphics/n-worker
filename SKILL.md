---
name: n-worker
description: >
  공용 코드 작업 워커. 기능 추가, 리팩토링, 데이터 연결 등 다단계 코드 작업을
  의도 확인, 플랜 승인, 구현, 실행 검증, 위험 기반 리뷰, 지식 기록까지 수행한다.
  사용자가 /n-worker 를 명시적으로 호출할 때 실행된다.
disable-model-invocation: true
---

# n-worker

요청한 변경을 프로젝트 구조 안에서, 검증 가능한 상태로 만든다. 조사와 리뷰는 작업의 위험에 맞춰 고른다. 비용에는 메인 대화뿐 아니라 서브의 초기 입력, 재시도, 백그라운드 기록이 포함된다.

## 진행 순서

| 단계 | 할 일 | 읽을 문서 |
|---|---|---|
| P0 컨텍스트 | 작업 무게, 프로젝트 지침, 관련 노트북 확인 | 아래 노트북 절 |
| P1 의도와 플랜 | 결과를 바꾸는 미정만 질문하고 짧은 플랜 작성 | [interview.md](references/interview.md), [plan.md](references/plan.md) |
| P2 착수 판단 | 플랜 승인, 위험과 승인 범위 확인 | 불확실한 고위험 설계만 [review.md](references/review.md) |
| P3 구현 | 가까운 기존 구현을 본떠 직접 처리하거나 독립 작업 위임 | [produce.md](references/produce.md) |
| P4 검증 | 컴파일/테스트, 데이터 확인, 실제 diff 리뷰 | [verify.md](references/verify.md), 리뷰가 필요하면 [review.md](references/review.md) |
| P5 마무리 | 변경과 검증 결과 보고, 필요한 지식 기록 | 기록이 있으면 [curator.md](agents/curator.md) |

짧은 작업(한두 파일의 국소 수정, 질문)은 파이프라인 없이 직접 처리한다. 다단계 작업은 `$WORK/plan.md`를 유지하며 바뀐 절만 갱신한다. 플랜은 Markdown 하나다. 사용자가 HTML/PDF 보고서를 요청하면 [reporter.md](agents/reporter.md)와 [report-render.md](references/report-render.md)를 사용한다.

### 의도와 승인

- 사용자에게 묻는 것은 의도, 정책, 되돌리기 어려운 선택이다. 코드로 알 수 있는 사실은 조사하고, 되돌리기 쉬운 구현 상세는 기존 패턴을 따른다.
- 다단계 작업은 착수 전에 플랜 요약(범위, 변경 방식, 위험, 검증 수단)을 대화에 출력하고 같은 턴에서 `AskUserQuestion`으로 승인을 한 번 받는다. 첫 옵션은 "이 플랜으로 시작"이다. 승인 뒤의 사소한 조정은 다시 묻지 않는다. 데이터 손실, 외부 쓰기, 공개 계약 변경, 큰 범위 확대는 작업 크기와 무관하게 착수 전 승인 대상이다.
- P1~P2에서 프로젝트 파일은 읽기만 한다. 편집은 승인 뒤 P3에서 시작한다.
- 질문은 `AskUserQuestion`으로 묶어서 내고 근거 있는 추천을 첫 옵션에 둔다. 질문할 것이 없으면 멈추지 않고 진행한다.
- 요청 전제와 실물이 모순되면(이미 구현됨, 대상이 없음) 근거를 제시하고 방향을 확인한다. 변경 없이 종료하는 것도 정당한 결과다.

### 검증과 리뷰

동작의 근거는 최신 소스에 대한 컴파일/테스트와 실제 산출물이다. 자동 검증이 놓치는 것만 사람이 읽어 확인한다: 기존 구조와 진입점 준수, 불필요한 헬퍼와 중복, 문자열 키와 리소스 참조, 실패 비용이 큰 경로. 저위험은 메인이 diff를 확인하고, 일반 작업은 통합 리뷰어 1명, 고위험은 전문 리뷰어 1명을 더한다. 범위와 반환 형식은 review.md가 정한다.

스택 단계 카드(컴파일 검증, 플레이 검증 등)는 트리거가 맞으면 검증 수단으로 사용한다. 에디터 모드 전환, 외부 데이터 쓰기, 다른 작업자의 미저장 상태에 영향을 주는 동작은 안전 조건과 승인을 먼저 확인한다.

실패를 수정한 뒤에는 실패했던 검사와 영향받는 회귀 검사만 다시 실행한다. 미실행/미검증은 결과 보고에 구분하고, 남은 위험이 완료를 막으면 차단 상태로 둔다.

## 노트북과 작업 폴더

1. 다단계 작업은 `scripts/nb-load.sh <프로젝트 루트 절대경로>`로 registry와 3레이어 INDEX를 읽는다. 출력의 `=== WORK:`가 작업 폴더다. 이후 명령/명세의 `$WORK`를 이 절대경로로 치환한다(셸 변수는 호출 사이에 유지되지 않는다). Windows는 출력의 `C:/...` 표기를 사용한다.
2. 관련 도메인 인덱스와 필요한 lesson만 읽는다. 노트북은 현재 코드의 대체물이 아니라 과거 조사 결과와 함정의 안내다. 값이나 구조에 동작을 걸기 전에는 라이브로 확인한다.
3. 새 작업 영역, 새 의존성, 새 실패 증상을 만났을 때 `scripts/nb-grep.sh "$WORK" <이름들...>`로 묶어 검색한다. 기존 파일 수정 시 파일 이름도 첫 검색에 포함한다. 결과와 관련 lesson은 플랜에 작업 단위로 남긴다. 이 스크립트 실행만 대조로 인정된다 - 손 grep과 기억은 게이트 기록을 남기지 않는다.
4. 대상과 노트북 내용이 같으면 검색 결과를 재사용한다. 새 대상, 기록 변경, 이전 결과와 모순되는 증상일 때 해당 범위만 갱신한다. 게이트(`scripts/nb-gate.sh`)가 거부하면 대상 이름을 검색하고 재시도한다. 게이트는 검색 기록을 확인하는 장치이지 코드 검증은 아니다.
5. 미등록 프로젝트는 로컬 지침과 라이브 근거로 진행한다. 등록 정보(슬러그, 스택과 버전, 지침 문서 위치)를 묶어 받아 curator `register`를 큐에 넣고 기다리지 않는다. 등록 전 기록 후보는 `$WORK`에 보관했다가 P5에서 함께 보낸다.
6. 서브를 쓰기 전에 [model-routing.md](notebook/common/model-routing.md)의 운영 표를 한 번 읽고 세션 내내 쓴다. 세션 모델이 표에 없거나 별칭이 거부될 때만 `model-refresh`를 큐에 넣는다. nb-load의 하네스 버전 변경 줄은 정보다 - 스크립트나 Workflow가 실제로 깨졌을 때 `harness-refresh`를 넣는다.

## 위임과 근거 재사용

- 위임 명세와 결과 확인의 비용보다 분리할 일이 충분히 클 때 서브를 쓴다. 짧은 Read/grep과 작은 수정은 메인이 묶어 처리한다. 파일 수보다는 독립된 질문/모듈을 기준으로 나눈다.
- 공유 심볼과 파일 소유권을 명세에 적는다. 공유 계약을 만드는 작업을 먼저 실행하고 소비 작업을 병렬화한다. 같은 파일을 수정하는 작업은 한 담당자가 맡는다.
- 서브에는 명세 파일 경로, 필요한 절, 관련 lesson 경로를 전달한다. 스킬/노트북 전문을 읽히면 독립 컨텍스트의 고정비가 서브마다 반복된다.
- 위임은 1단이다. 범위가 크면 확인한 결과와 남은 질문을 반환받고 메인이 다음 일을 정한다. 최초 리뷰는 독립적으로, 수정 확인은 기존 서브와 근거를 재사용한다.
- 모델은 별칭(`'opus'`, `'sonnet'`)으로 명시하고 역할별 effort는 model-routing을 따른다. 버전 id는 어느 파일에도 박지 않는다. Workflow 입력/출력 계약은 review.md §4에 있다.
- 서브 보고의 근거 위치와 확인 범위를 받는다. 메인은 고위험 변경, 모순, 근거 부족 항목을 재확인한다. 변경되지 않은 근거와 신뢰할 수 있는 실행 결과는 재사용한다.
- 일시적 실행 실패는 해당 단위만 1회 재시도한다. 반복 실패는 원인을 해결하거나 검증 범위를 조정한다. 완료 알림(task-notification)을 기다리며, 개별 Agent의 `TaskOutput`은 트랜스크립트를 대량 반환할 수 있어 결과 수집에 쓰지 않는다.
- 빌드/테스트 전문은 `$WORK` 로그에 저장하고 발췌만 읽는다. Workflow는 건수와 완료 여부만 반환하고 전문은 `scripts/wf-summarize.sh <journal>`로 요약한다. 누락된 결과와 미검증 범위를 먼저 확인한다.

## 기록과 마무리

- P5에서 변경 파일과 내용, 실행한 검증, 남은 일을 프로젝트 보고 형식에 맞춰 알린다.
- 재조사 비용이 큰 사실과 사용자가 알려준 외부 정책만 기록한다. 코드에서 바로 확인되는 구조, 일회성 상태, 일반 개발 지식은 저장 효과가 작다.
- 작업당 기록 요청은 하나로 묶는다. 제안마다 내용 요약, 메커니즘과 근거(file:line 또는 사용자 발화), 제안 레이어를 싣고 재사용 스크립트 경로를 더한다. curator는 제안을 옮겨 적고 인용 위치만 확인하므로, 메커니즘이 빠진 제안은 근거 없는 기록이 된다. 기록과 정비의 범위, 실행 예산은 curator.md가 정한다.
- `scripts/curator-enqueue.sh <요청.json>` 뒤에는 작업을 계속한다. 노트북은 큐의 curator 한 주체가 쓴다. 기록 실패는 다음 로딩의 알림으로 확인하며 메인이 대신 수정하지 않는다. 이 스킬 자체를 시험하는 세션(eval/dogfood)은 노트북에 기록하지 않는다 - 드러난 결함은 스킬 파일로 고친다.
- 산출물을 열어달라는 요청에는 `scripts/open-artifact.sh <절대경로>`를 사용한다.

## 작업 보호

- 프로젝트의 진입점과 구조 밖에 우회로, 중복 시스템, 하드코딩 키를 만들지 않는다. 프로젝트 facts가 불변으로 명시한 규칙은 사용자 요청으로도 넘지 않는다 - 선택지는 포기 또는 구조 안으로 수정이다.
- 프로젝트 facts에 전용 스킬이 명시된 영역은 그쪽에 양보한다. 지침이 요구하는 도구가 환경에 없으면 시작할 때 한 줄로 알리고, 가능한 검사와 미검증 부분을 구분한다.
- 다른 작업자의 미커밋 변경과 기존 진입점을 보존한다. 결과는 세션 시작 상태와 비교하며, 커밋은 사용자가 요청할 때 수행한다. 외부 쓰기는 직전 상태와 부분 실패 후 실제 결과를 확인한다.
